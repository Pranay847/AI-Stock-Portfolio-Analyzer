# sentence-transformers pulls in torch. Without this index the resolver picks
# the CUDA build and drags in the whole nvidia-* stack (several GB), which blows
# past the Streamlit Community Cloud resource limit. The CPU wheel is all this
# app needs -- embeddings run on CPU.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

langchain>=1.0.0
langchain-community>=0.4.0
langchain-core>=1.0.0
langchain-ollama>=1.0.0
langchain-mistralai>=1.0.0
langgraph>=1.2.0
mcp>=1.0.0
faiss-cpu>=1.12.0
# xgboost declares nvidia-nccl-cu12 on Linux (~400 MB of NCCL for distributed
# GPU training this app never does). xgboost-cpu is the same library and the
# same `import xgboost`, minus the CUDA dependency.
xgboost-cpu>=2.1.1
pandas>=2.3.0
numpy>=2.3.0
yfinance>=0.2.33
streamlit>=1.40.0
python-dotenv==1.0.0
scikit-learn>=1.7.0
plotly>=5.18.0
sentence-transformers>=3.0.0
ollama>=0.4.4,<1.0.0

# Added for OpenAI embeddings, model access and utilities
openai>=0.27.0
langchain-openai>=0.3.0
tiktoken>=0.4.0
joblib>=1.3.2
tqdm>=4.66.1
requests>=2.31.0
chromadb>=0.4.22
robin-stocks>=3.0.0
